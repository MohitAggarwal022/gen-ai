# commands used

--------------------------------------------------------------------------------------

# git commands

git checkout branch


git init
git add "folder name"
git commit -m "msg"
git push origin main

if code committed to master instead of branch

git log --oneline --all --graph
git log --oneline -3   // get 3 latest logs

git checkout branch

// always older first (in reverse order)
git cherry-pick id here for last one 
like
git cherry-pick 453212w

git checkout master

git reset --hard HEAD~3



# to add gitignore
npx gitignore python
--------------------------------------------------------------------------------------

# cmd used in python code

python3 -m venv venv
source venv/bin/activate

link folder/.env folder/.env

pip install openai python-dotenv requests
pip install google-genai
python "filename" to run the application

pip freeze > abc.txt ###### list all installed python packages with versions
pip install langchain_community pypdf
pip install langchain-text-splitters
pip install langchain-openai
pip install langchain-qdrant

pip install "fastapi[standard]"
### run fastapi code and server will running on port 8000
fastapi dev server.py

pip install rq
pip install uvicorn
pip install pydantic


# to run the worker/consumer
rq worker --with-scheduler 

rq worker --with-scheduler --url redis://valkey:6379

sh worker.sh

--------------------------------------------------------------------------------------


# docker commands

docker compose -f 'file path here'
 docker compose up
 docker compose up - d
 docker compose down


 --------------------------------------------------------------------------------------

# langgraph


pip install langgraph 


# for pdf - pypdf
pip install langchain_community pypdf

# split chunk by chunk
RecursiveCharacterTextSplitter
pip install langchain-text-splitters

# add qdrant
pip install langchain-qdrant

# qdrant wrapper in langchain
QdrantVectorStore

# langchain open ai embedding
OpenAIEmbeddings
pip install langchain-openai


pip install mem0ai
pip install langchain-neo4j
pip install langfuse

### STATE IN langgraph

TypedDict
StateGraph
START 
END

## for routing
Literal

## for add multiple data just like array list
Annotated, add_messages

## easily change the model provide as well
init_chat_model


####### CHECKPOINTER
pip install pymongo langgraph langgraph-checkpoint-mongodb

## to save checkpointer in mongo db
MongoDBSaver


 --------------------------------------------------------------------------------------


Tool Calling 

1. create function annotated with @tool decorator
2. bind tool using .bind_tools method
3. 


use @tool()

ToolNode, tools_condition = present in prebuilt package

 --------------------------------------------------------------------------------------


#####   vector db

- PineCone
- Astra db
- qdrant
- Chroma db
- weaviate db
- PG vector
- Milvus db


create a docker compose file to run qdrant db
loclhost:6333/dashboard = local url for qdrant


- valkey (redis alternative) - run on port 6379 (redis defaukt port)


--------------------------------------------------------------------------------



export $(grep -v '^#' .env | xargs -d '\n') 
rq worker --with-scheduler --url redis://valkey:6379